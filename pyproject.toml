[tool.poetry]
name = "data-science-browser"
version = "0.1.0"
description = ""
authors = ["thomas0barand <th.barand@gmail.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
matplotlib = "^3.10.7"
scikit-learn = "^1.7.2"
torch = "^2.0.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "ipykernel (>=7.1.0,<8.0.0)"
]
